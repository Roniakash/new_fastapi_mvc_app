import os

# Folder to store geojson files
OUTPUT_DIR = "geojson_files"
os.makedirs(OUTPUT_DIR, exist_ok=True)

# Folder to store map HTML files
MAP_DIR = "MAP_file"
os.makedirs(MAP_DIR, exist_ok=True)
